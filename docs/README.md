# Kyle documentation

The [root README](../README.md) covers the backend: the agent, the turn pipeline, the
services, auth and deployment. These pages cover the web app and the data behind it.

- [Design system](design-system.md) — tokens, primitives, and the rules that keep the
  views looking like one app.
- [Frontend](frontend.md) — structure of `web/`, routing, the API layer, and what
  "mobile first" actually constrains here.
- [Dashboard](dashboard.md) — where every figure on the home screen comes from, what
  it approximates, and the Plex behaviour that will bite you.
- [Testing and tooling](testing.md) — the one command, the three gates, what earns a
  test, and where the gaps are.
