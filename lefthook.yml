pre-commit:
  parallel: true
  commands:
    fmt:
      run: bunx oxfmt --check .
    lint:
      run: bunx oxlint .
    typecheck-server:
      run: bunx tsc --noEmit -p tsconfig.server.json
    typecheck-web:
      run: cd web && bunx vue-tsc --noEmit
